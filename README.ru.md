# Bad CRM

**Self-hosted multi-tenant рабочее пространство для команд разработки.** Проекты, канбан-задачи,
блочные документы, markdown-база знаний, файлы, тайм-трекинг, дашборды, командный чат,
E2EE-хранилище паролей, защищённые ссылки, статусы GitHub Actions и AI-ассистент — в одной
инсталляции, с единой моделью данных, единой моделью прав и одной командой `docker compose up`.

Лицензия — **AGPL-3.0-or-later**. Интерфейс на **английском и русском**.

[English README](README.md)

---

> ## ⚠️ Состояние проекта: фаза проектирования — приложения ещё нет
>
> В репозитории лежит **спецификация и декомпозиция работ** (продуктовые и архитектурные документы,
> ADR, дизайн безопасности, правила разработки, эпики и пользовательские истории), а начиная с
> EPIC-001 — **среда разработки**: pnpm-workspace с четырьмя пакетами (`shared`, `server`,
> `client`, `e2e`), кешируемый turborepo-pipeline, общая строгая конфигурация TypeScript, ESLint 9
> flat config с архитектурными запретами, `docker-compose.yml` с зависимыми сервисами
> (PostgreSQL + pgvector, Redis, MinIO, Meilisearch, Mailpit) и `.env.example` с zod-схемами
> окружения. Реальный код пока только в `shared` — zod-примитивы, branded id, каталог permissions
> и коды ошибок.
>
> Приложения по-прежнему нет: ни HTTP-эндпоинтов, ни интерфейса, ни Prisma-схемы, ни релиза, ни
> Docker-образа самого приложения. `docker compose up` поднимает сервисы, которые приложению
> понадобятся, а не приложение.
>
> Всё, что описано ниже, — **запланировано**, а не сделано. Установка self-hosted в разделе
> «Быстрый старт» — **проектируемый интерфейс**, а не то, что можно запустить сегодня; рядом с ней
> описан сценарий разработки, который на чистом клоне действительно работает.
>
> Если вы ищете софт, который можно поставить прямо сейчас, — это пока не он. Если вам интересно,
> как он проектируется, или вы хотите помочь его построить, — читайте дальше.

---

## Скриншот

<!-- Плейсхолдер: скриншот дашборда появится здесь, когда появится интерфейс (M2+). -->

*Скриншотов пока нет — показывать нечего. Раздел будет заполнен, как только появится работающий
интерфейс.*

---

## Что это

Bad CRM — рабочее пространство для команд разработки примерно от 5 до 50 человек. Он существует
потому, что сегодня этот контекст размазан по шести-семи несвязанным SaaS-сервисам: задача живёт в
одном инструменте, её спецификация — в другом, обсуждение — в третьем, доступы к стенду — в
четвёртом, потраченные часы — в пятом.

Ключевая ставка — не «ещё один трекер задач», а **связность**: проект и сотрудник — две центральные
модели, к которым пристёгнуто всё остальное: задачи, документы, знания, файлы, секреты, время, чат,
пайплайны и деньги. Один граф, один поиск, одна модель прав, одна установка.

Слово «CRM» здесь означает управление отношениями с **заказчиком проекта разработки** (контракт,
сроки, приёмка, инвойсы), а не продажи.

## Для кого

| Роль | Что получает |
|---|---|
| **Разработчик** | Задачи, спецификации, чат и таймер в одном месте; полный контекст задачи без переключения инструментов |
| **Тимлид** | Планирование, ревью, загрузка и отчёты по времени в разрезе людей и проектов |
| **Проектный лидер / PM** | Клиенты, контракты, бюджет против burn, приёмка вех, звонки и принятые на них решения |
| **Администратор организации** | Одно место для онбординга и офбординга человека, одна модель прав для выдачи и отзыва доступа |
| **Владелец инсталляции** | Данные на своём железе, отсутствие поштучной оплаты, возможность форкнуть и проаудировать |

## Чем принципиально не является

Не sales-CRM (нет воронок и лидов), не helpdesk для внешних клиентов, не HR-система (нет
рекрутинга, ревью и компенсаций), не конструктор произвольных BI-отчётов, не маркетплейс плагинов.
И не хостится нами: SaaS-версии нет и не планируется.

---

## Возможности

Всё в этом разделе — **запланировано**. Колонка «Майлстоун» показывает, когда это стоит в плане;
что означает каждый майлстоун — см. [дорожную карту](#дорожная-карта).

| Домен | Что запланировано | Майлстоун |
|---|---|---|
| **Организации и мультиарендность** | Несколько организаций на инсталляцию, изоляция на уровне БД через PostgreSQL Row Level Security | M1 |
| **Аутентификация** | Регистрация организации, сессии с ротацией refresh и детектом повторного использования, восстановление пароля, TOTP-двухфакторка | M1, M2 |
| **Люди и права** | Профили сотрудников, роли, персональные override прав, ACL на объекты, журнал аудита | M2 |
| **Проекты** | Карточка проекта, участники, статусы, связи со всем остальным в системе | M2 |
| **Файлы** | S3-совместимое хранилище (MinIO или любой S3), presigned-загрузка, скачивание с проверкой прав | M2 |
| **Задачи** | Канбан-доски и колонки, карточка задачи, комментарии, вложения, доступ на уровне задачи | M3 |
| **Документы** | Notion-подобные блочные документы с версионированием | M4 |
| **База знаний** | Obsidian-подобные markdown-заметки с обратными ссылками и графом | M4 |
| **Поиск** | Мгновенный, устойчивый к опечаткам поиск по всем доменам с учётом прав | M4 |
| **Чат и уведомления** | Slack-подобные каналы, богатые сообщения, presence, realtime-уведомления | M5 |
| **Учёт времени** | Таймер и ручные записи, таймшиты, процесс согласования | M6 |
| **Дашборды** | Дашборды по проектам, людям и деньгам; drill-down по сотруднику | M6 |
| **Хранилище секретов** | E2EE-хранилище паролей — сервер хранит только шифротекст и не может его прочитать | M7 |
| **Защищённые ссылки** | Одноразовые и ограниченные ссылки для передачи секрета человеку | M7 |
| **Интеграция с GitHub** | Чтение статусов Actions, деплоев и коммитов, связанных с задачами | M8 |
| **AI-ассистент** | Чат и поиск по своим данным на том LLM-провайдере, которого выберет админ (Anthropic, OpenAI, любой OpenAI-совместимый endpoint, OpenRouter) | M8 |
| **Онбординг** | Материалы и чек-листы онбординга для новых участников | M8 |
| **Проектное лидерство** | Клиенты, контракты, инвойсы, бюджет против burn, спринты и поставка, звонки и календарь | M9 |

Два компонента **опциональны по замыслу** — приложение обязано стартовать и работать без них, с
деградацией функций, а не отказом: Meilisearch (поиск падает на полнотекст PostgreSQL) и AI
(функции скрыты в UI и возвращают внятное «функция выключена»).

---

## Быстрый старт

### Установка приложения — пока недоступна

> Self-hosted альфа выходит в **EPIC-017**, в конце майлстоуна M2. Образа приложения, который можно
> вытянуть, пока нет: `docker-compose.yml` в репозитории поднимает **зависимые сервисы** для
> разработки (PostgreSQL, Redis, MinIO, Meilisearch, Mailpit), но не приложение. Команды ниже —
> проектируемый интерфейс, записанный здесь, чтобы дизайн можно было отревьюить; рабочей установки
> они сегодня не дадут.

#### Требования (планируемые)

| | Минимум | Рекомендуется |
|---|---|---|
| CPU / RAM | 2 vCPU / 2 GB (профиль `minimal`) | 2 vCPU / 4 GB (профиль `default`) |
| Диск | 10 GB + ваши данные | SSD с запасом под файлы и бэкапы |
| Софт | Docker 24+ с Compose v2 | То же плюс reverse-proxy с TLS |
| ОС | Linux (x86-64 или arm64) | То же |

#### Как это задумано

```bash
git clone https://github.com/code0verdose/bad-crm.git
cd bad-crm
cp .env.example .env
# сгенерировать обязательный ключ шифрования
openssl rand -base64 32          # → APP_ENCRYPTION_KEY
docker compose up -d
```

Дальше — открыть приложение, создать первую организацию и стать её владельцем. Полная процедура,
включая обязательный чек-лист безопасности после установки, — в
[`docs/runbooks/install.md`](docs/runbooks/install.md).

Профиль `minimal` не поднимает Meilisearch и запускает воркеры внутри процесса API — для небольших
инсталляций на скромном железе.

### Работа с кодом — работает сегодня

Требования: **Node 22 LTS** (`>=22.22.2`, в `.nvmrc` зафиксирован 22.22.2), **pnpm 10** (ставится Corepack из поля
`packageManager`), Git. Docker опционален: проверки ниже проходят без него, он нужен только чтобы
поднять зависимые сервисы.

```bash
git clone https://github.com/code0verdose/bad-crm.git
cd bad-crm
corepack enable                          # подтянет pnpm 10.x, зафиксированный в package.json
pnpm install                             # установит все четыре пакета workspace
pnpm turbo run typecheck lint build test # полный набор проверок, обязательный перед push
pnpm dev                                 # все пакеты в watch-режиме, Ctrl+C останавливает всё дерево
```

`pnpm dev` сейчас запускает три параллельных watcher'а: сборку `shared` (`tsc -b --watch`), скелет
сервера (`tsx watch src/main.ts`, печатает одну строку старта) и watcher типов клиента.

| Команда | Состояние сегодня |
|---|---|
| `pnpm install`, `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm dev` | работают |
| `pnpm lint`, `pnpm format:check` | работают — ESLint 9 flat config с архитектурными запретами и Prettier |
| `pnpm docker:up`, `pnpm docker:down`, `pnpm docker:logs`, `pnpm docker:reset` | работают — поднимают и гасят зависимые сервисы, а не приложение |
| `pnpm test:integration`, `pnpm test:e2e`, `pnpm db:migrate`, `pnpm db:seed`, `pnpm api:gen` | заведены в turbo, но ни один пакет их пока не реализует (EPIC-003, EPIC-010) |

Чтобы поднять сервисы локально, скопируйте шаблон и заполните плейсхолдеры:

```bash
cp .env.example .env
openssl rand -base64 32   # → APP_ENCRYPTION_KEY
openssl rand -base64 48   # → JWT_SECRET
pnpm docker:up            # PostgreSQL + pgvector, Redis, MinIO, Meilisearch, Mailpit
```

К этим сервисам в репозитории пока никто не подключается — сервер всё ещё скелет, печатающий одну
строку. Они подняты для того, чтобы первой миграции и первому эндпоинту было куда приземлиться.

Сам контракт репозитория (состав workspace, направление зависимостей, контракт tsconfig,
compose-файл и согласованность `.env.example` со схемами окружения) покрыт тестами в
[`test/`](test/) — они выполняются в составе `pnpm test`.

Конвенции, архитектура и текущая декомпозиция работ — в [`docs/`](docs/), [`rules/`](rules/) и
[`epics/`](epics/); начинать стоит с [`CONTRIBUTING.md`](CONTRIBUTING.md).

---

## Конфигурация

Конфигурация — только переменные окружения, разобранные схемой один раз при старте. Ошибка
конфигурации означает отказ старта, а не 500 через час работы.

**Обязательные:**

| Переменная | Что это |
|---|---|
| `APP_URL` | Публичный URL инсталляции; используется для CORS, cookie и ссылок в письмах |
| `DATABASE_URL` | Строка подключения к PostgreSQL под ролью приложения (она **не** должна иметь `BYPASSRLS`) |
| `REDIS_URL` | Строка подключения к Redis |
| `JWT_SECRET` | Секрет подписи короткоживущих access-токенов, минимум 32 символа |
| `APP_ENCRYPTION_KEY` | 32 байта в base64 — шифрует секреты интеграций at rest. **Потеря ключа делает сохранённые креды интеграций невосстановимыми** |
| `S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY`, `S3_SECRET_KEY` | Объектное хранилище для файлов |

**Опциональные (отсутствие деградирует функцию, но не ломает старт):**

| Переменная | Что будет, если не задана |
|---|---|
| `SMTP_URL` | Письма пишутся в лог (dev) / почтовые операции падают с внятной ошибкой (prod) |
| `MEILI_HOST`, `MEILI_MASTER_KEY` | Поиск падает на полнотекст PostgreSQL; в UI баннер об ограниченных возможностях |
| `AI_ENABLED` | AI-функции скрыты в UI и возвращают `feature_disabled` |
| `OTEL_EXPORTER_OTLP_ENDPOINT` | Трейсы не экспортируются; логи и метрики работают |
| `LOG_LEVEL` | По умолчанию `info` |

**API-ключи LLM-провайдеров никогда не живут в переменных окружения.** Их вводит администратор
организации в интерфейсе, и хранятся они зашифрованными в базе. `AI_ENABLED` — только
общеинсталляционный выключатель.

Полный список с комментариями и безопасными дефолтами будет в `.env.example` (создаётся в EPIC-001).

---

## Архитектура

```
                Браузер (React 19 + Vite)
                 ─ единственное место, где vault расшифрован
                          │ HTTPS / WebSocket
                          ▼
              Reverse-proxy (TLS, sticky /socket.io)
                          │
        ┌─────────────────┴──────────────────┐
        │   Образ приложения (один образ)    │
        │   ROLE=api          ROLE=worker    │
        │   Express 5, гексагональная арх.   │
        └─────────────────┬──────────────────┘
                          │
   ┌──────────┬───────────┼───────────┬─────────────┐
   ▼          ▼           ▼           ▼             ▼
PostgreSQL   Redis      MinIO    Meilisearch      SMTP
16+pgvector  7          (S3)     (опционально)
изоляция     очереди,   файлы    поиск
через RLS    pub/sub
```

Ключевые решения, у каждого свой ADR:

- **Мультиарендность через PostgreSQL Row Level Security**, а не через «мы не забыли добавить
  `WHERE`» — второй, независимый рубеж ниже приложения ([ADR-0004](docs/architecture/adr/0004-multi-tenancy-postgres-rls.md)).
- **Гексагональный backend**: доменные правила и политики доступа чистые и тестируются без БД,
  инфраструктура заменяема ([ADR-0002](docs/architecture/adr/0002-hexagonal-backend-express-prisma.md)).
- **Contract-first API**: OpenAPI 3.1 — источник истины, правится руками в ревью; типы клиента
  генерируются из спеки, двусторонний контрактный тест ломает сборку при расхождении ([ADR-0003](docs/architecture/adr/0003-openapi-as-source-of-truth.md)).
- **Транзакционный outbox**: доменные события пишутся в той же транзакции, что и данные, и уже потом
  разъезжаются по очередям — ничего внешнего внутри транзакции ([ADR-0021](docs/architecture/adr/0021-transactional-outbox.md)).
- **Zero-knowledge vault**: ключи выводятся и используются только в браузере; сервер держит
  шифротекст и метаданные и не может расшифровать ничего ([ADR-0009](docs/architecture/adr/0009-e2ee-vault-key-hierarchy.md)).

Вся документация: [`docs/README.md`](docs/README.md) — начинать оттуда.
Архитектурный обзор: [`docs/architecture/overview.md`](docs/architecture/overview.md).
Все 21 решение: [`docs/architecture/adr/`](docs/architecture/adr/).

---

## Дорожная карта

Девять майлстоунов от пустого репозитория до self-hosted релиза 1.0. Майлстоун закрывается только
при выполнении критерия выхода целиком; недоделки не переносятся хвостом.

| Майлстоун | Цель | Эпиков | Статус |
|---|---|---|---|
| **M1** | Фундамент: монорепо, CI и commit-гейт, скелет сервера и контракт API, оболочка клиента, RLS-мультиарендность, вход, дизайн-система, EN/RU, наблюдаемость, e2e-харнесс | 10 | Не начат |
| **M2** | Люди, права, проекты, файлы: RBAC, управление сотрудниками, TOTP, ядро проекта, файловое хранилище, журнал аудита, первая self-hosted альфа | 7 | Не начат |
| **M3** | Задачи: доски и колонки, ядро задач, совместная работа, доступ на уровне задачи | 4 | Не начат |
| **M4** | Знания: блочные документы, база знаний, поиск с учётом прав | 3 | Не начат |
| **M5** | Realtime: инфраструктура реального времени, ядро чата, богатые сообщения, уведомления | 4 | Не начат |
| **M6** | Время и аналитика: учёт времени, таймшиты и согласование, дашборды, drill-down по сотруднику | 4 | Не начат |
| **M7** | Секреты: крипто-фундамент E2EE, элементы хранилища, шаринг, защищённые ссылки | 4 | Не начат |
| **M8** | Интеграции и AI: GitHub, администрирование AI-провайдеров, AI-ассистент, материалы онбординга | 4 | Не начат |
| **M9** | Лидерство и релиз: клиенты и контракты, биллинг и бюджет, спринты и поставка, звонки и календарь, security hardening, self-hosted 1.0 | 6 | Не начат |

Всего 46 эпиков. Подробный состав майлстоунов и критерии выхода —
[`docs/product/roadmap.md`](docs/product/roadmap.md). Файлы эпиков и историй — [`epics/`](epics/).

---

## Участие в разработке

Вклад приветствуется — проект находится в той фазе, когда обратная связь по дизайну ценнее кода.
Перед пул-реквестом прочитайте [`CONTRIBUTING.md`](CONTRIBUTING.md). Коротко:

- Работа идёт в рамках эпика и истории; кода без них не бывает.
- **TDD обязателен** — сначала падающий тест.
- Каждый коммит проходит гейт из шести проверок и правила из [`rules/`](rules/).
- Коммиты — Conventional Commits, на английском.
- Используется **DCO** (`Signed-off-by`), а не CLA.

Также прочитайте [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md).

## Безопасность

**Не** сообщайте об уязвимостях через публичные issue. Канал приёма, сроки ответа, скоуп и политика
раскрытия — в [`SECURITY.md`](SECURITY.md).

Гарантии хранилища секретов — и, что не менее важно, чего оно **не** гарантирует — описаны в
[`docs/security/e2ee-design.md`](docs/security/e2ee-design.md) и
[`docs/security/threat-model.md`](docs/security/threat-model.md). Обе стоит прочитать прежде, чем
доверять свои секреты любому продукту, включая этот.

## Лицензия

**GNU Affero General Public License v3.0 или новее (AGPL-3.0-or-later).** Полный текст: [`LICENSE`](LICENSE).

Что это практически означает для того, кто хостит Bad CRM:

- Вы можете запускать его для любых целей, включая коммерческие, и изменять как угодно.
- Если вы **предоставляете изменённую версию пользователям по сети**, раздел 13 AGPL требует
  предложить этим пользователям полный соответствующий исходный код вашей изменённой версии. Запуск
  неизменённой копии для своей команды такой обязанности не порождает — достаточно сохранить
  лицензию и уведомления.
- Распространение софта, изменённого или нет, влечёт обычные обязательства семейства GPL:
  доступность исходников, сохранение лицензии и копирайт-уведомлений, та же лицензия на всё
  произведение.
- CLA нет — значит, проект нельзя перелицензировать за спиной контрибьюторов.

Зависимости ограничены AGPL-совместимыми лицензиями; BSL, SSPL, Elastic License, Commons Clause,
«free for non-commercial» и GPL-2.0-only исключены, проверка лицензий — часть CI. См.
[ADR-0018](docs/architecture/adr/0018-license-agpl-3.md).

Это краткое изложение, а не юридическая консультация — читайте саму лицензию.
