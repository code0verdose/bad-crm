---
id: STORY-006-04
epic: EPIC-006
status: backlog
blocked: false
priority: should
estimate: M
---

# STORY-006-04 — Logout и управление активными сессиями

**Как** разработчик (пользователь продукта) **я хочу** видеть, где открыты мои сессии, и закрывать
любую из них **чтобы** после входа с чужого ноутбука не оставлять доступ к рабочим данным.

## Acceptance (Given/When/Then)

- **Given** активная сессия **When** нажимаю «Выйти» **Then** текущая сессия отзывается, refresh-cookie очищается, `sid` попадает в денилист, клиент переходит на `/login`, а кеш запросов очищается полностью.
- **Given** страница `/settings/security` **When** я её открываю **Then** вижу список активных сессий: устройство и браузер (из user-agent), приблизительное расположение или IP в маскированном виде, время создания и последней активности, отметку «текущая».
- **Given** список сессий **When** отзываю чужую сессию **Then** она немедленно перестаёт работать: следующий запрос с её access-токеном получает 401 (денилист), а refresh отклоняется.
- **Given** отзыв текущей сессии **When** я подтверждаю действие в диалоге **Then** выполняется выход, как при обычном logout; без подтверждения действие не выполняется.
- **Given** кнопка «Завершить все другие сессии» **When** я её нажимаю **Then** отзываются все сессии, кроме текущей, и в списке остаётся одна запись.
- **Given** IP-адрес **When** он отображается **Then** он показан в соответствии с политикой приватности (хранится хеш, отображается маскированное значение), а полный IP в UI не раскрывается.
- **Given** истёкшие сессии **When** отрабатывает фоновая очистка **Then** они удаляются или архивируются, а список не показывает мусор.
- **Given** отзыв сессии **When** он произошёл **Then** он зафиксирован в аудите с актором, целевой сессией и IP.

## Задачи

- [ ] Написать тесты первыми: `application/identity/use-cases/logout.use-case.test.ts`, `list-sessions.query.test.ts`, `revoke-session.use-case.test.ts` (нельзя отозвать чужую сессию другого пользователя — 404), `test/integration/auth/session-revocation.test.ts` (немедленная недействительность access-токена).
- [ ] Реализовать `POST /api/v1/auth/logout`, `GET /api/v1/auth/sessions`, `DELETE /api/v1/auth/sessions/{id}`, `POST /api/v1/auth/sessions/revoke-others`; описать в `openapi.yaml`.
      *(2026-07-28: **все четыре описаны** в спеке с маркером `x-implemented-by: STORY-006-04`.
      Чужая или несуществующая сессия — 404 `session_not_found` (новый ресурс в каталоге кодов), не
      403. Отзыв своей текущей сессии разрешён и очищает cookie; подтверждение — диалог клиента, а
      не второй endpoint. Список не пагинируется сознательно, обоснование в схеме `SessionList`.
      Реализация — за этой историей.)*
      *(2026-07-28, ревизия: `SessionSummary` описывает **семейство**, а не строку — ротация создаёт
      новую строку `Session`. Поэтому `lastUsedAt` — это `created_at` живой строки (момент последней
      ротации), а `createdAt` — `min(created_at)` по семейству; `Session.updatedAt` в контракте
      больше не участвует: `@updatedAt` считает клиент Prisma, и сырой `UPDATE … WHERE family_id`
      его не двигает. Оба поля стали обязательными.)*
- [x] **~~Блокирует acceptance «IP в маскированном виде»~~ — закрыто 2026-07-28.** Было: модель
      давала `Session` только `ipHash`, расхешировать который для показа нельзя, поэтому
      `SessionSummary.ipMasked` объявили опциональным. Стало: у `Session` есть колонка
      `ip_masked TEXT NOT NULL` (маскирование выполняется **при входе, до записи**: IPv4 → /24,
      IPv6 → /48, функция `packages/server/src/domain/identity/mask-ip-address.util.ts`, для
      нечитаемого входа — `unknown`), поле контракта **обязательное**, `ipHash` остался для
      сопоставления сессий между собой. Полный адрес не хранится нигде. Сделано до релиза и до
      первой строки сознательно: после релиза это навсегда nullable-колонка и навсегда
      опциональное поле. См. `docs/architecture/data-model.md` → «Про адрес сессии».
- [x] **~~Реестру маршрутов нужна третья форма записи~~ — закрыто 2026-07-28.** В
      `route-registry.types.ts` есть третий член союза `SelfServiceRoute` (`selfService: true` +
      `selfServiceReason` + **обязательный** `ownershipCheckedIn`); предикаты
      `requiresAuthentication`/`requiresPermission` задают поведение гардов машинно — guard
      аутентификации остаётся, guard прав пропускается. `openapi.test.ts` сверяет маркер спеки с
      формой реестра **в обе стороны** (`authorizationFormOf` ↔ `specAuthorizationForm`), словарь
      маркеров один на оба теста. Само объявление маршрута появится вместе с контроллером этой
      истории.
- [ ] **Остаток той же задачи, в чужих файлах:** снять запись «третья форма — работа EPIC-011» из
      `docs/api/README.md` («Что EPIC-006 оставил открытым», п. 2 — утверждение теперь ложно) и
      описать форму в `docs/security/permission-model.md` §3.20 рядом с собственной ключевой парой
      пользователя, откуда она и взята. Оба файла — вне дельты, закрывшей код.
- [ ] Реализовать парсер user-agent в человекочитаемое описание устройства (без внешних сервисов).
- [ ] Реализовать хранение `ip_hash` и запись `ip_masked` на входе.
      *Уточнено ревизией 2026-07-28:* прежняя формулировка «функция маскирования для отображения»
      противоречила физике хеша — маскировать на показе нечего, наружу из БД приходит `ip_hash`.
      Маскирование выполняется **при входе, до записи**; функция и колонка уже есть (пункт выше),
      этой истории остаётся вызвать её в use-case'е входа и заполнить обе колонки.
- [ ] Реализовать очистку истёкших сессий фоновой задачей (repeatable job) с записью метрики.
- [ ] Реализовать клиентский экран `/settings/security`: список сессий, действия, `ConfirmDialog` для разрушающих операций, полная очистка кеша при logout.
- [ ] Добавить события в аудит-журнал (заготовка порта — [EPIC-009](../../epic-009-observability/epic.md)).

## Definition of Done

- [ ] Тесты написаны первыми (TDD), проходят, изменённый код покрыт
- [ ] Commit-гейт зелёный (test-coverage, security-auditor, db-reviewer при изменении схемы, production-readiness, commit-hygiene)
- [ ] Документация обновлена (docs/ + запись в `docs/brain/`)
- [ ] a11y-проверка: таблица сессий с заголовками, действия доступны с клавиатуры, подтверждение с ловушкой фокуса
- [ ] i18n: строки в обоих языках, хардкода нет

## Ссылки

- Документация: [`ux-architecture.md` → `/settings/security`, Подтверждение разрушающих действий](../../../docs/architecture/ux-architecture.md), [`stack.md` → Токены и сессии](../../../docs/architecture/stack.md), [`prd.md` → NFR-12](../../../docs/product/prd.md)
- Правила: `rules/security.mdc`, `rules/a11y.mdc`, `rules/i18n.mdc`
