---
id: STORY-006-08
epic: EPIC-006
status: review
blocked: false
priority: should
estimate: M
---

# STORY-006-08 — Сброс пароля по email с одноразовым токеном

**Как** разработчик (пользователь продукта) **я хочу** восстановить доступ по ссылке из письма
**чтобы** забытый пароль не требовал обращения к администратору инсталляции.

## Acceptance (Given/When/Then)

- **Given** существующий email **When** я запрашиваю сброс **Then** на почту уходит письмо с одноразовой ссылкой; в БД сохраняется только хеш токена, TTL — 60 минут.
- **Given** несуществующий email **When** я запрашиваю сброс **Then** ответ API идентичен успешному (текст «если адрес зарегистрирован, письмо отправлено»), письмо не отправляется — существование аккаунта не раскрывается.
- **Given** валидная ссылка **When** я открываю `/reset-password/$token` и задаю новый пароль **Then** пароль меняется, токен помечается использованным, **все** сессии пользователя отзываются, происходит автоматический вход или переход на `/login` с сообщением об успехе.
- **Given** уже использованный токен **When** я открываю ссылку повторно **Then** показывается сообщение о недействительности ссылки с предложением запросить новую; пароль не меняется.
- **Given** просроченный токен **When** я открываю ссылку **Then** тот же исход, что и при использованном, — без раскрытия, какой именно случай произошёл.
- **Given** новый пароль, не удовлетворяющий политике **When** я отправляю форму **Then** 422 с ошибкой по полю, токен остаётся действительным до истечения TTL.
- **Given** dev-окружение без реального SMTP **When** запрошен сброс **Then** письмо видно в Mailpit, ссылка рабочая.
- **Given** production без настроенного `SMTP_URL` **When** запрошен сброс **Then** операция завершается понятной ошибкой (`mail_not_configured`), а не молча «успехом» без письма.
- **Given** повторные запросы сброса для одного адреса **When** их больше лимита **Then** срабатывает rate limiting, ответ по-прежнему не раскрывает существование аккаунта.

## Задачи

- [x] Написать тесты первыми: `application/identity/use-cases/request-password-reset.use-case.test.ts` (неразличимость ответов, отправка письма только для существующего), `confirm-password-reset.use-case.test.ts` (успех, повторное использование, TTL, слабый пароль), `test/integration/auth/password-reset.test.ts` (хеш токена в БД, отзыв всех сессий).
      *Сделано 2026-07-29:* 16 + 14 юнит-проверок, `test/integration/http/password-reset.test.ts`
      (17, сравнивает два ответа поле за полем — статус, тело, `content-type`), и живая суита на
      настоящем PostgreSQL и настоящем Mailpit. **Путь другой:** `test/integration/db/password-reset.test.ts`,
      а не `test/integration/auth/…` — каталог `test/integration/auth/` заводить нельзя, пока
      `rules/security.mdc` обещает в нём `refresh-reuse.spec.ts` и `rate-limit.spec.ts`: как только
      каталог существует, обещания из «abstained» становятся «missing» и
      `test/rules/declared-checks.test.ts` краснеет. Лечится записью в `PENDING` того теста или
      правкой самих формулировок — обе вне границ этой правки.
      *Одна из проверок переписана после мутации.* Первая версия гонки погашения запускала оба
      `UPDATE` из `pg` и утверждала, что один вернул ноль строк, — это проверка PostgreSQL, а не
      кода: она осталась зелёной, когда `consume` был подменён на `findFirst` + безусловный
      `UPDATE`, потому что репозиторий в ней не участвовал. Теперь конкурент — `pg`, а субъект —
      репозиторий: конкурент держит блокировку строки, `consume` встаёт в очередь, конкурент
      коммитит, и правильная реализация возвращает `null`. На мутации падает.
- [x] Добавить модель токена сброса (таблица с `token_hash`, `user_id`, `expires_at`, `used_at`) и миграцию с блоком RLS.
      *Сделано 2026-07-28:* сущность `PasswordResetToken` сперва дописана в
      [`data-model.md` §1](../../../docs/architecture/data-model.md) (её там не было — пробел
      спецификации), затем реализована в миграции `20260728120000_auth_core_identity_and_sessions`:
      `password_reset_tokens` с полным RLS-блоком, составным FK `(organization_id, user_id)` на
      `users` и глобально уникальным `uq_password_reset_tokens_hash`. Хранится только SHA-256
      токена; погашение — `UPDATE … WHERE id = $1 AND used_at IS NULL RETURNING id`, поэтому
      повторное использование отвергается строкой, а не проверкой в коде. **Остаётся:**
      `SECURITY DEFINER`-функция резолва токена до организации — она идёт вместе с use-case'ом этой
      истории и с расширением `01-grants.sql` на `pg_proc` из STORY-006-02. **Владелец — не
      `app_auth`, а `app_auth_definer`** (уточнено 2026-07-29): тело исполняется с правами владельца,
      а у `app_auth` нет ни одной табличной привилегии, поэтому функция в её владении упала бы
      `permission denied for table password_reset_tokens` изнутри себя. Функции также нужен
      `COMMENT ON FUNCTION … IS 'bad-crm:auth-resolver …'`, а таблице — строка в `definer_reads`
      файла `01-grants.sql`: без маркера восстановление после `pg_restore` её не чинит, без
      `definer_reads` — не даёт владельцу `SELECT`.
- [x] Реализовать `infrastructure/mail/nodemailer.adapter.ts` под портом `MailerPort`; при отсутствии `SMTP_URL` — адаптер, пишущий в лог (dev) и падающий с `mail_not_configured` (prod).
      *Частично сделано 2026-07-29:* `MailPort`, `NodemailerMailAdapter`, `UnconfiguredMailAdapter`,
      `createMailer`, интеграционный тест против живого Mailpit. **«Пишущий в лог (dev)» отвергнут:**
      письмо этой истории несёт одноразовый токен внутри ссылки, а лог — самое непубличное место в
      деплое только на словах; без `SMTP_URL` не пишется ничего и операция отвечает
      `503 mail_not_configured` в любом окружении. Мейлер пока нигде не собирается — композиционный
      корень его не строит, потому что use-case'а этой истории ещё нет.
      *Дозакрыто 2026-07-29 (гейт продакшен-готовности), три правки — все три о том, что письма не
      ушли бы ни на одном настоящем релее:*
      (1) **адреса отправителя не было ни в `OutgoingMail`, ни в схеме окружения.** Mailpit
      принимает пустой `MAIL FROM` — поэтому интеграционный тест был зелёным, — а Postfix, SES и
      любой релей с проверкой конверта отвечают 5.x. Добавлена переменная `MAIL_FROM`, обязательная
      кросс-полевым правилом ровно тогда, когда задан `SMTP_URL` (разово обязательной делать нельзя:
      `rules/self-host-packaging.mdc`, правило 2). Значение живёт на **адаптере**, а не в
      `OutgoingMail`: из какого ящика отправляет инсталляция — свойство релея оператора, а не
      решение use-case'а, и каждый будущий вызывающий иначе обязан был бы про него помнить. Поле
      `from` в `TransportMessage` сделано **обязательным**, так что сообщение без него не
      компилируется. Схема отвергает перевод строки в значении — оно уходит в заголовок письма.
      (2) **`EENVELOPE` маппился в `recipient_rejected`.** Оператора, у которого релей отверг
      *отправителя*, отправляли разбираться с получателем — ровно не та половина конверта, и как раз
      на том отказе, который dev-Mailpit не воспроизводит никогда. Метка переименована в
      `envelope_rejected`: nodemailer поднимает один код на обе половины.
      (3) **таймауты не задавались.** Родные значения nodemailer — 2 мин на соединение, 30 с на
      приветствие и **10 мин** на сокет; релей, который принял TCP и замолчал, держал бы поток
      запроса всё это время. Проставляются 10 с / 10 с / 30 с — в query-строку URL, потому что
      `createTransport(url, defaults)` вторым аргументом принимает умолчания **сообщения**, а
      объект с ключом `url` рядом с опциями эти опции выбрасывает (`nodemailer/lib/nodemailer.js`).
      Значение оператора, если он его задал, не перезаписывается; переписывается только query-часть,
      credential-часть URL не трогается вовсе (round-trip через `new URL()` — это нормализация, а
      нормализация пароля тихо делает из него другой пароль).
      *Закрыто 2026-07-29 (эта правка): мейлер собран.* `createMailer` вызывается в
      `container.factory.ts`, поверх него — `ImmediateMailDispatcher`, шаг остановки `mail`
      регистрируется **безусловно** (оба адаптера имеют `close()`, поэтому он не становится
      условным в день, когда почту выключат) и дренирует переданные сообщения перед закрытием
      транспорта.
- [x] Реализовать шаблоны письма на EN и RU (текст + HTML), без секретов в теме письма и с абсолютной ссылкой на `APP_URL`.
      *Сделано 2026-07-29:* `domain/identity/password-reset-mail.util.ts` и
      `password-changed-mail.util.ts`, язык выбирается из `users.locale`
      (`mail-locale.util.ts`, всё, что не русское, — английское: письмо на языке, которого читатель
      может не знать, лучше, чем отсутствие письма). Токен — только в пути ссылки: не в теме
      (её видно в уведомлениях и на локскрине) и не в query (её пишет в лог каждый прокси и
      подставляет в `Referer`). Ссылка абсолютная от `APP_URL`, хвостовые слэши нормализуются.
      Имя организации в письме экранируется — это единственное значение в шаблоне, пришедшее от
      человека, и в HTML-части оно иначе было бы разметкой.
- [x] Реализовать endpoints `POST /api/v1/auth/forgot-password` и `POST /api/v1/auth/reset-password`, описать в `openapi.yaml`.
      *(2026-07-28: **оба описаны**, маркер `x-implemented-by: STORY-006-08`. `forgot-password`
      отвечает 202 без тела независимо от того, есть ли адрес — телу просто негде отличаться;
      `organizationSlug` в запросе сознательно нет, адрес в нескольких организациях получает по
      письму на аккаунт. **Токен сброса — поле тела** `reset-password`, не query: query-строка
      уезжает в логи прокси и в `Referer`; контрактный тест
      `route-authorization.test.ts` падает на любом параметре пути/query с именем вида
      `token`/`password`/`secret`. Неизвестный, использованный и просроченный токен — один ответ
      `400 password_reset_token_invalid`. Успех отзывает **все** сессии и не выдаёт новых токенов.
      Добавлен код `mail_not_configured` (503) — граница с `feature_disabled` описана в
      `docs/api/README.md`.)*
      *(2026-07-28, ревизия: граница двух кодов перенесена в сам каталог
      `packages/shared/src/errors/error-code.enums.ts`, где до этого стояли два взаимоисключающих
      утверждения — SMTP числился в перечне выключаемых подсистем `feature_disabled` и одновременно
      объявлялся не им. Перечень исправлен; 501 — «сервер такого не умеет, ретрай бессмыслен», 503 —
      «временно, ретрай осмыслен». **Требование к реализации:** отсутствие SMTP проверяется **до**
      резолва адреса, иначе 503 отличает существующий адрес от несуществующего и ломает свойство
      «ответ постоянен», на котором построена вся операция. Записано в описание операции в
      `openapi.yaml`.)*
- [x] **Закрыть находки серверного аудита безопасности** (2026-07-29, отдельный проход после реализации).
      *H1 — Argon2id считался до погашения токена.* Хеш вычислялся **до** `tokens.consume`, поэтому
      повторное предъявление одной живой ссылки покупало полноценный KDF на каждый запрос: резолвер
      находил строку, аллоцировалось 19 MiB, и только потом выяснялось, что строка уже погашена. На
      бюджете `api_request` это 300 KDF в минуту с адреса — примерно в 900 раз шире, чем 5/15 мин,
      которые `rules/security.mdc` правило 11 даёт логину, то есть тому же примитиву. Комментарий в
      коде утверждал обратное («дорогой путь ограничен выданными токенами») — утверждение было
      неверным и удалено. **Выбран вариант «сначала погасить, потом хешировать», целиком внутри одной
      транзакции**, а не «строгий бюджет 5/15 на подтверждение»: бюджет уменьшил бы усиление, но не
      убрал бы его (ссылка по-прежнему переигрывается неограниченно, просто пять раз за окно), и
      ключа для него нет — у этой половины потока нет email, а `auth_attempt` типизирован парой
      «адрес + email»; бюджет по одному адресу — это пять восстановлений на весь офис за NAT, ровно
      тот отказ, ради которого `rate-limit.port.ts` и выбрал пару. После перестановки число KDF,
      которое может вызвать адрес, равно числу токенов, выданных ему `forgot-password`, а тот считает
      их на `auth_attempt` 5/15 по паре — то есть число правила 11 работает, просто шагом раньше.
      **Риск «токен потрачен, пароль не записан» разобран и снят конструкцией:** он существует только
      если погашение коммитится отдельно от записи пароля; здесь `consume`, хеш и оба `UPDATE` — одна
      транзакция, поэтому падение процесса посреди хеша откатывает погашение (ссылка остаётся
      рабочей), а падение после коммита — это состоявшийся сброс с потерянным 204, что закрывает
      `Idempotency-Key`. Цена — блокировка строки токена на время Argon2id; она ограничена тем же
      числом живых токенов, потому что до хеша доходит только успешно погашенный токен.
      *Тест, закреплявший ошибку, переписан:* блок «the budget of the confirm half» утверждал
      300/мин как достаточную величину; теперь он объясняет, что ограничивает дорогую ветку не этот
      счётчик, а порядок, и рядом стоит блок «the cost of a token that cannot be spent» — 20 повторов
      одной ссылки дают ровно один Argon2id.
      *M4 — успешный сброс и смена пароля не гасили остальные выданные токены.* Добавлен
      `PasswordResetTokenRepositoryPort.invalidateOutstanding`, вызывается **в той же транзакции** и
      в подтверждении сброса, и в смене пароля; число погашенных ссылок пишется в событие
      (`revokedResetTokens`). Сессии закрывались, а более сильный credential — ссылка, выпускающая
      новый пароль, — жил ещё до часа.
      *L3 — «успех» при нулевом обновлении.* `updatePasswordHash` теперь возвращает `boolean`
      (`updateMany` матчил ноль строк молча). Ре-хеш при логине по-прежнему игнорирует ноль; сброс и
      смена пароля делают из него наблюдаемый отказ: строка лога уровня `error` с новым событием
      `password_not_written` и откат транзакции — сброс отвечает своим единственным
      `400 password_reset_token_invalid` (иначе отдельный код сказал бы держателю ссылки, что токен
      был настоящий), смена — `401 invalid_credentials`, тем же кодом, что и ветка выше.
      *L6 — подтверждение не перепроверяло статус учётки.* После погашения читается `users.findById`
      (он же отсекает мягко удалённых), и не-`ACTIVE` отвечает тем же единственным отказом. Токен при
      этом **остаётся потраченным**: ссылка, выпущенная активному аккаунту, не должна ожить в день
      его разблокировки.
      *TTL-очистка.* `deleteSettled` удаляет использованные и просроченные строки аккаунта в той же
      транзакции, что и выдача новой ссылки: планировщика нет (нет ни `outbox_event`, ни воркера
      BullMQ), а таблица не чистилась вообще — при том что каждая строка в ней хеш высланного
      credential'а. Это гигиена, а не защита: безвредной устаревшую ссылку делает предикат `consume`,
      до которого теперь ничего дорогого не происходит. **Открыто:** глобальный свод по всем
      аккаунтам — за эпиком очереди.
      *L4 — не было умолчания `requireTLS` для SMTP.* `smtpUrlWithTimeouts` переименован в
      `smtpUrlWithDefaults` и проставляет `requireTLS=true` для `smtp://` — **кроме loopback**
      (`localhost`, `127.0.0.1`, `[::1]`), где стоит Mailpit из `.env.example` и профиля `default`,
      TLS не умеющий; `smtps://` не трогается (там TLS с первого байта). Явное значение оператора
      (`requireTLS`, `ignoreTLS`, `secure`) выигрывает — это же и путь обновления для инсталляции с
      релеем без TLS. **Открыто:** упомянуть умолчание в `.env.example` и `docs/runbooks/upgrade.md`
      (вне границ этой правки).
      *L2 — `err: error` в логе диспетчера почты.* Поле убрано, вместо него `errorName` — класс
      брошенного значения. Утечка была невидима для существующих проверок: у `Error` нет перечислимых
      свойств, поэтому `JSON.stringify` давал `{}` и все ассерты «адреса в логе нет» проходили мимо, а
      pino своим `err`-сериализатором пишет `message`, который релей составляет, цитируя конверт.
- [ ] Реализовать клиентские экраны `/forgot-password` и `/reset-password/$token` с состояниями «отправлено», «ссылка недействительна», «успех».
- [x] Подключить отправку письма через очередь (постановка задачи, ретраи) — либо синхронно с явным TODO-переносом в эпик очередей, зафиксированным в ADR.
      *Сделано 2026-07-29 вторым вариантом, и компромисс назван, а не спрятан.* Ни таблицы
      `outbox_event`, ни BullMQ-очереди в дереве нет, поэтому заведён порт
      `application/platform/ports/mail-dispatch.port.ts` и адаптер
      `infrastructure/mail/immediate-mail-dispatcher.adapter.ts`: use-case коммитит, затем
      **передаёт** сообщение и возвращается — сокет открывается уже после его возврата.
      **Что сохранено:** внешнего вызова внутри транзакции нет (`rules/outbox.mdc`, 2), и ответ
      `forgot-password` не ждёт релея — иначе ветка с существующим адресом отличалась бы временем
      ответа, и постоянство ответа держалось бы только на статусе.
      **Что отдано до появления очереди:** ретраев нет (упавший релей теряет письмо, человек просит
      заново, а уже созданная строка просто истекает через час); долговечности через рестарт нет;
      dead-letter, куда мог бы посмотреть администратор, нет — вместо неё строка лога на каждый
      исход, включая неудачные. Полный текст сделки — в шапке порта. Замена адаптера на
      outbox-handler не меняет ни одного use-case'а: они зовут `dispatch` и на этом заканчивают.
      **ADR не заведён:** решение принято внутри уже принятого ADR-0021 (очередь `mail` там уже
      описана как целевая), и заводить ADR ради «пока делаем без него» значит фиксировать
      отсутствие как решение. Если нужен отдельный ADR — это открытый пункт.
- [ ] Добавить события в аудит: запрошен сброс, пароль сброшен.
      *(2026-07-29: события пишутся полем `event` из `SECURITY_EVENTS` —
      `passwordResetRequested` (на **обе** ветки, с `accounts: 0` для неизвестного адреса, иначе
      оракул просто переезжает из ответа в лог), `passwordResetCompleted`, `passwordResetRefused`
      (один на все три отказа, **без** поля, различающего их). Адреса в строках нет, токена нет,
      источник маскирован до /24 или /48. Таблицы `AuditLog` в схеме ещё нет (EPIC-009) — писатель
      возьмёт то же поле.)*

## Definition of Done

- [ ] Тесты написаны первыми (TDD), проходят, изменённый код покрыт
- [ ] Commit-гейт зелёный (test-coverage, security-auditor, **db-reviewer обязателен**, production-readiness, commit-hygiene)
- [ ] Документация обновлена (docs/ + запись в `docs/brain/`)
- [ ] a11y-проверка: сообщения о состоянии объявляются live-region, форма доступна с клавиатуры
- [ ] i18n: строки в обоих языках, хардкода нет — включая тему и тело письма

## Ссылки

- Документация: [`ux-architecture.md` → Публичная зона](../../../docs/architecture/ux-architecture.md), [`stack.md` → Почта, деградация при отсутствии SMTP](../../../docs/architecture/stack.md), [`threat-model.md`](../../../docs/security/threat-model.md)
- Правила: `rules/security.mdc`, `rules/i18n.mdc`
