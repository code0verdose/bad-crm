---
id: STORY-008-02
epic: EPIC-008
status: in-progress
blocked: false
priority: must
estimate: S
---

**Частично закрыта 2026-07-30.** Сделано: цепочка «профиль → браузер → английский»
(`shared/i18n/language.util.ts`, 11 тестов), хук `useLanguage` — сохранение выбора, немедленное
применение через `i18n.changeLanguage`, синхронизация `<html lang>` (6 тестов, включая «нет мигания
английского»: значение берётся на первом рендере, а не в эффекте). Язык писем с сервера был закрыт
ещё в EPIC-006 (`mailLocaleOf` от `users.locale`).

**Осталось: сам переключатель, и остановлено это на архитектурном решении, а не на нехватке времени.**
Компонент был написан и откачен. Правило слоёв запрещает импорт вглубь `shared/ui`, относительный
родительский импорт — тоже, поэтому `CenteredScreen` **не может** композировать `LanguageControl`. А
публичные экраны — именно тот случай, где переключатель нужнее всего: человек, который не читает язык
формы входа, не может войти, чтобы добраться до настройки.

Выбор из трёх — положить оба компонента в одну папку, поднять композицию на слой выше, или смягчить
правило — это решение, и его стоит принять на свежую голову. Монтирование в топбар отдельно тоже не
проходит: ломает 17 тестов, считающих содержимое оболочки, и их правка — та же спешка.


# STORY-008-02 — Переключатель языка, сохранение и определение локали

**Как** разработчик (пользователь продукта) **я хочу** выбрать язык интерфейса один раз **чтобы**
он применялся сразу, сохранялся между сессиями и переносился на другие устройства.

## Acceptance (Given/When/Then)

- **Given** первый визит без сохранённого выбора **When** браузер сообщает `Accept-Language: ru-RU` **Then** интерфейс открывается на русском; при неизвестном языке — на английском.
- **Given** порядок определения локали **When** он применяется **Then** используется цепочка: профиль пользователя → `localStorage` → `navigator.language` → `en`; каждый шаг покрыт тестом.
- **Given** переключатель языка в меню аватара или на `/settings/appearance` **When** я меняю язык **Then** интерфейс переключается мгновенно без перезагрузки, `<html lang>` обновляется, выбор сохраняется в `localStorage`.
- **Given** аутентифицированный пользователь **When** он меняет язык **Then** выбор сохраняется в профиле (после [EPIC-012](../../epic-012-employee-management/epic.md)); до тех пор — только локально, и это явно отражено в интерфейсе.
- **Given** публичный экран `/login` **When** пользователь не аутентифицирован **Then** переключатель языка доступен и работает — профиля ещё нет.
- **Given** сохранённая локаль **When** страница перезагружается **Then** правильный язык применяется до завершения загрузки сессии (без «мигания» английского).
- **Given** серверные письма (например, сброс пароля) **When** они отправляются **Then** язык письма берётся из профиля пользователя, а при его отсутствии — из языка организации, затем `en`.

## Задачи

- [ ] Написать тесты первыми: `units/appearance/service/hooks/use-locale.hook.test.tsx` (цепочка определения, переключение, персист), `test/i18n/no-flash.test.tsx` (нет мигания языка при загрузке), `shared/i18n/detect-locale.test.ts`.
- [ ] Реализовать `src/shared/i18n/detect-locale.ts` с описанной цепочкой и whitelist поддерживаемых локалей.
- [ ] Реализовать `src/units/appearance/ui/language-switcher.component.tsx` и хук `use-locale.hook.ts`.
- [ ] Применять локаль до рендера приложения (чтение `localStorage` в `index.html`/раннем модуле) для предотвращения мигания.
- [ ] Обновлять `<html lang>` при смене локали.
- [ ] Добавить переключатель на публичные экраны и в меню аватара.
- [ ] Реализовать серверный выбор языка письма (`Accept-Language`/профиль/организация) для сценариев auth.
- [ ] Подготовить точку интеграции с профилем пользователя (`PATCH /api/v1/me` с полем `locale`) и описать её в `openapi.yaml`.

## Definition of Done

- [ ] Тесты написаны первыми (TDD), проходят, изменённый код покрыт
- [ ] Commit-гейт зелёный (test-coverage, security-auditor, db-reviewer при изменении схемы, production-readiness, commit-hygiene)
- [ ] Документация обновлена (docs/ + запись в `docs/brain/`)
- [ ] a11y-проверка: переключатель — доступный элемент управления с текстовой меткой, смена языка объявляется
- [ ] i18n: строки в обоих языках, хардкода нет

## Ссылки

- Документация: [`ux-architecture.md` → Направление и переключатель](../../../docs/architecture/ux-architecture.md), [`prd.md` → NFR-8](../../../docs/product/prd.md)
- Правила: `rules/i18n.mdc`, `rules/a11y.mdc`
