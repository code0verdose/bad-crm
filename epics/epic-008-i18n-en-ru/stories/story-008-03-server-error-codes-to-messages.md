---
id: STORY-008-03
epic: EPIC-008
status: backlog
blocked: false
priority: must
estimate: M
---

# STORY-008-03 — Серверные ошибки как коды и их перевод

**Как** разработчик (пользователь продукта) **я хочу** видеть понятные сообщения об ошибках на
своём языке **чтобы** понимать, что пошло не так, без чтения английских технических формулировок.

## Acceptance (Given/When/Then)

- **Given** ответ сервера `problem+json` с `code: "task_forbidden"` **When** клиент его обрабатывает **Then** пользователю показывается сообщение из `errors.json` текущей локали; `title` и `detail` с сервера не показываются.
- **Given** неизвестный клиенту код ошибки **When** он приходит **Then** показывается общее сообщение «Что-то пошло не так» с кодом в мелком тексте для обращения в поддержку, а не пустая строка и не сырой JSON.
- **Given** ошибка валидации 422 со списком `errors[]` **When** она обрабатывается **Then** сообщения привязываются к конкретным полям формы по `path`, а не выводятся одним тостом.
- **Given** каталог кодов в `packages/shared` **When** запускается тест **Then** каждому коду соответствует ключ в `errors.json` **обоих** языков; отсутствие пары валит сборку.
- **Given** новый код ошибки, добавленный на сервере **When** для него нет перевода **Then** CI падает до мержа, а не после релиза.
- **Given** сообщение об ошибке с параметрами (например, лимит и время ожидания при 429) **When** оно отображается **Then** значения подставляются интерполяцией, а не конкатенацией строк.
- **Given** ошибка сервера **When** она логируется **Then** технические поля (`detail`, `requestId`) уходят в лог/консоль dev, но не в интерфейс пользователя.

## Задачи

- [ ] Написать тесты первыми: `shared/lib/errors/message-map.test.ts` (перевод по коду, fallback для неизвестного, интерполяция), `test/i18n/error-codes-parity.test.ts` (каталог кодов ↔ ключи в `en/errors.json` и `ru/errors.json`), `units/*/форма` (привязка `errors[]` к полям).
- [ ] Реализовать `src/shared/lib/errors/error-message.util.ts` — выбор ключа по `code`, fallback, интерполяция параметров.
- [ ] Заполнить `en/errors.json` и `ru/errors.json` полным набором кодов из `packages/shared/errors/codes.ts`.
- [ ] Реализовать сопоставление `errors[].path` с полями формы Mantine (утилита `apply-server-errors.util.ts`).
- [ ] Подключить перевод к `notifyApiError` из [STORY-007-03](../../epic-007-design-system/stories/story-007-03-toaster-wrapper.md) и к `ErrorState`.
- [ ] Добавить в CI тест паритета кодов и ключей; включить его в тот же шаг, что и проверку паритета ключей локалей.
- [ ] Задокументировать правило «сервер отдаёт код, клиент выбирает текст» в `docs/architecture/stack.md` (при необходимости уточнить) и в `rules/i18n.mdc`.

## Definition of Done

- [ ] Тесты написаны первыми (TDD), проходят, изменённый код покрыт
- [ ] Commit-гейт зелёный (test-coverage, security-auditor, db-reviewer при изменении схемы, production-readiness, commit-hygiene)
- [ ] Документация обновлена (docs/ + запись в `docs/brain/`)
- [ ] a11y-проверка: сообщения об ошибках связаны с полями и объявляются live-region
- [ ] i18n: строки в обоих языках, хардкода нет

## Ссылки

- Документация: [`stack.md` → Формат ошибок `application/problem+json`](../../../docs/architecture/stack.md), [`ux-architecture.md` → Обработка ошибок](../../../docs/architecture/ux-architecture.md)
- Правила: `rules/i18n.mdc`, `rules/api-contract.mdc`
