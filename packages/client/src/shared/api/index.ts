/**
 * The contract, as types. `api-schema.d.ts` is generated from `docs/api/openapi.yaml` by
 * `pnpm api:gen` and is never edited by hand; the HTTP client that consumes it lands with
 * STORY-004-06, which is why nothing here has a runtime value yet.
 */
export type * from './schemas/api-schema.js';
